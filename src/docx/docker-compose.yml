services:
  docx:
    build:
      context: ../..
      dockerfile: src/docx/Dockerfile
    ports:
      - "8005:8005"
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/docx/src
    environment:
      - TRANSPORT=sse
      - PORT=8005
