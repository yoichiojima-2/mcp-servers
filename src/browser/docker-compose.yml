services:
  browser:
    build:
      context: ../..
      dockerfile: src/browser/Dockerfile
    ports:
      - "8007:8007"
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/browser/src
    environment:
      - TRANSPORT=sse
      - PORT=8007
