services:
  browser:
    build:
      context: ../..
      dockerfile: src/browser/Dockerfile
    ports:
      - "8001:8001"
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/browser/src
    environment:
      - TRANSPORT=sse
      - PORT=8001
      - HEADLESS=${HEADLESS:-true}
