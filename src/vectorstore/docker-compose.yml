services:
  vectorstore:
    build: .
    ports:
      - "8002:8002"
    volumes:
      - ./workspace:/workspace
      - ./src:/vectorstore/src
    environment:
      - TRANSPORT=sse
    command: make serve
