services:
  o3:
    build:
      context: ../..
      dockerfile: src/o3/Dockerfile
    ports:
      - "8007:8007"
    volumes:
      - ./workspace:/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8007
      - OPENAI_API_KEY=${OPENAI_API_KEY}
