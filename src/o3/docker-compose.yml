services:
  o3:
    build:
      context: ../..
      dockerfile: src/o3/Dockerfile
    ports:
      - "8009:8009"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8009
      - OPENAI_API_KEY=${OPENAI_API_KEY}

volumes:
  mcp-workspace:
