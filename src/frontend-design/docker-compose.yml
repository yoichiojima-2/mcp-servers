services:
  frontend-design:
    build:
      context: ../..
      dockerfile: src/frontend-design/Dockerfile
    ports:
      - "8005:8005"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8005

volumes:
  mcp-workspace:
