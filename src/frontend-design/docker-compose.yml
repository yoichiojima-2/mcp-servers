services:
  frontend-design:
    build:
      context: ../..
      dockerfile: src/frontend-design/Dockerfile
    ports:
      - "8009:8009"
    volumes:
      - ./workspace:/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8009
