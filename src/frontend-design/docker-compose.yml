services:
  frontend-design:
    build:
      context: ../..
      dockerfile: src/frontend-design/Dockerfile
    ports:
      - "8006:8006"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8006

volumes:
  mcp-workspace:
