services:
  pptx:
    build:
      context: ../..
      dockerfile: src/pptx/Dockerfile
    ports:
      - "8009:8009"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8009

volumes:
  mcp-workspace:
