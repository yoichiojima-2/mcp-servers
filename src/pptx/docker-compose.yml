services:
  pptx:
    build:
      context: ../..
      dockerfile: src/pptx/Dockerfile
    ports:
      - "8011:8011"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8011

volumes:
  mcp-workspace:
