services:
  pptx:
    build:
      context: ../..
      dockerfile: src/pptx/Dockerfile
    ports:
      - "8003:8003"
    environment:
      - TRANSPORT=sse
      - PORT=8003
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/pptx/src
