services:
  pptx:
    build:
      context: ../..
      dockerfile: src/pptx/Dockerfile
    ports:
      - "8010:8010"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8010

volumes:
  mcp-workspace:
