services:
  data-analysis:
    build:
      context: ../..
      dockerfile: src/data-analysis/Dockerfile
    ports:
      - "8002:8002"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8002

volumes:
  mcp-workspace:
