services:
  data-analysis:
    build:
      context: ../..
      dockerfile: src/data-analysis/Dockerfile
    ports:
      - "8002:8002"
    volumes:
      - data-analysis-workspace:/root/.mcp-servers/data-analysis
    environment:
      - TRANSPORT=sse
      - PORT=8002

volumes:
  data-analysis-workspace:
