services:
  skills:
    build:
      context: ../..
      dockerfile: src/skills/Dockerfile
    ports:
      - "8005:8005"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
      - ./src:/app/src/skills/src
    environment:
      - TRANSPORT=sse
      - PORT=8005

volumes:
  mcp-workspace:
