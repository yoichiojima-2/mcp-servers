services:
  pdf:
    build:
      context: ../..
      dockerfile: src/pdf/Dockerfile
    ports:
      - "8008:8008"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
      - ./src:/app/src/pdf/src
    environment:
      - TRANSPORT=sse
      - PORT=8008

volumes:
  mcp-workspace:
