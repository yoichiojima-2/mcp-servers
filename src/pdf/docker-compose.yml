services:
  pdf:
    build:
      context: ../..
      dockerfile: src/pdf/Dockerfile
    ports:
      - "8001:8001"
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/pdf/src
    environment:
      - TRANSPORT=sse
      - PORT=8001
