services:
  shell:
    build:
      context: ../..
      dockerfile: src/shell/Dockerfile
    ports:
      - "8012:8012"
    volumes:
      - shell-workspace:/root/.mcp-servers/shell
    environment:
      - TRANSPORT=sse
      - PORT=8012

volumes:
  shell-workspace:
