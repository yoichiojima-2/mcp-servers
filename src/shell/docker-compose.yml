services:
  shell:
    build:
      context: ../..
      dockerfile: src/shell/Dockerfile
    ports:
      - "8011:8011"
    volumes:
      - ./workspace:/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8011
