services:
  shell:
    build:
      context: ../..
      dockerfile: src/shell/Dockerfile
    ports:
      - "8012:8012"
    volumes:
      - ./workspace:/workspace
    environment:
      - TRANSPORT=sse
      - PORT=8012
