services:
  file-management:
    build:
      context: ../..
      dockerfile: src/file-management/Dockerfile
    ports:
      - "8005:8005"
    volumes:
      - file-management-workspace:/root/.mcp-servers/file-management
    environment:
      - TRANSPORT=sse
      - PORT=8005

volumes:
  file-management-workspace:
