services:
  langquery:
    build:
      context: ../..
      dockerfile: src/langquery/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./workspace:/workspace
      - ./src:/app/src/langquery/src
    environment:
      - TRANSPORT=sse
      - PORT=8000
