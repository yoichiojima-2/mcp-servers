services:
  langquery:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./workspace:/workspace
      - ./src:/langquery/src
    environment:
      - TRANSPORT=sse
    command: make serve
