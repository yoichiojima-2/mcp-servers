# composite server configuration
# enable/disable servers by setting enabled: true/false

servers:
  data-analysis:
    enabled: false
    prefix: data
    module: data_analysis
    description: data querying with DuckDB SQL

  xlsx:
    enabled: false
    prefix: xlsx
    module: xlsx
    description: excel spreadsheet operations

  pdf:
    enabled: false
    prefix: pdf
    module: pdf
    description: pdf document operations

  docx:
    enabled: false
    prefix: docx
    module: docx
    description: word document operations

  pptx:
    enabled: false
    prefix: pptx
    module: pptx_mcp
    description: powerpoint operations

  vectorstore:
    enabled: false
    prefix: vec
    module: vectorstore
    description: vector database operations

  browser:
    enabled: false
    prefix: browser
    module: browser
    description: browser automation

  frontend-design:
    enabled: false
    prefix: design
    module: frontend_design
    description: design principles, themes, and color palettes

  nano-banana:
    enabled: false
    prefix: img
    module: nano_banana
    description: AI image generation with Google Gemini

  dify:
    enabled: false
    prefix: dify
    module: dify
    description: Dify workflow integration

  file-management:
    enabled: true
    prefix: file
    module: file_management
    description: File read/write operations

  img2pptx:
    enabled: false
    prefix: img2pptx
    module: img2pptx
    description: Convert images to PPTX using GPT-5.2

  o3:
    enabled: false
    prefix: o3
    module: o3_search
    description: O3 AI-powered web search

  preview:
    enabled: false
    prefix: preview
    module: preview
    description: HTML preview server with live reload

  shell:
    enabled: true
    prefix: shell
    module: shell
    description: shell command execution with allowlist security

  skills:
    enabled: true
    prefix: skills
    module: skills
    description: Claude skills discovery and loading

