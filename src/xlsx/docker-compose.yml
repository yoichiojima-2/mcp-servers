services:
  xlsx:
    build:
      context: ../..
      dockerfile: src/xlsx/Dockerfile
    ports:
      - "8016:8016"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
      - ./src:/app/src/xlsx/src
    environment:
      - TRANSPORT=sse
      - PORT=8016

volumes:
  mcp-workspace:
