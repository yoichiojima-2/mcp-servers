services:
  xlsx:
    build:
      context: ../..
      dockerfile: src/xlsx/Dockerfile
    ports:
      - "8015:8015"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
      - ./src:/app/src/xlsx/src
    environment:
      - TRANSPORT=sse
      - PORT=8015

volumes:
  mcp-workspace:
