services:
  xlsx:
    build:
      context: ../..
      dockerfile: src/xlsx/Dockerfile
    ports:
      - "8013:8013"
    volumes:
      - mcp-workspace:/root/.mcp-servers/workspace
      - ./src:/app/src/xlsx/src
    environment:
      - TRANSPORT=sse
      - PORT=8013

volumes:
  mcp-workspace:
